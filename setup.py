import os

from setuptools import setup, find_packages
setup(
    name="OpStrings",
    version="1.0",
    description="OpStrings Project",
    packages=find_packages(),
    include_package_data=True
)